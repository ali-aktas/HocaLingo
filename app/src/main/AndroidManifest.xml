<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions for Firebase and API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Storage permission for downloaded word packages -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ✅ Notification permissions for daily reminders -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ✅ NEW: AlarmManager permissions for exact alarms (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- ✅ WorkManager & Alarm permissions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ✅ BILLING - Required for RevenueCat & In-App Purchases -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- ✅ AD_ID Permission - Keep as is for AdMob -->
    <!-- AdMob needs AD_ID, so we DON'T remove it -->
    <!-- <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" /> -->

    <application
        android:name=".HocaLingoApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HocaLingo"
        tools:targetApi="31">

        <!-- ✅ AdMob App ID (Test ID - Replace in Production!) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- ✅ Fix for AD_SERVICES_CONFIG conflict (AdMob vs Firebase) -->
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- Main Activity with splash screen theme -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme"
            android:screenOrientation="portrait"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- ✅ Handle notification deep links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="hocalingo" />
            </intent-filter>
        </activity>

        <!-- ✅ NEW: Daily Notification Receiver (AlarmManager based) -->
        <receiver
            android:name=".core.notification.DailyNotificationReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <!-- Custom action for daily notifications -->
                <action android:name="com.hocalingo.app.ACTION_DAILY_NOTIFICATION" />
                <!-- System boot completed to reschedule alarms -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>